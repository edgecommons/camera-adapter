# Camera Adapter Session Handoff — 2026-07-11

User requested a stop after verification of the Core Python native-streaming fix so the Windows
machine can reboot. Resume this file first. Do not discard or reset the existing dirty worktrees.

## Stop point

The Core Python native binding fix is rebuilt and its focused regression passes. All active agents
have stopped. The camera-adapter local implementation and its local quality gates are complete.

## Completed camera-adapter evidence

| Gate | Result |
|---|---|
| Formatting | `cargo +stable fmt --all -- --check` passed |
| Native common tests | `cargo +1.85.0 test --locked --no-default-features --lib -- --test-threads 1`: 232 passed |
| Native ONVIF tests | `cargo +1.85.0 test --locked --no-default-features --features standalone,onvif --lib -- --test-threads 1`: 295 passed, 1 ignored |
| Strict Clippy | Common and `standalone,onvif` library/tests both passed with `-D warnings` |
| Linux coverage, exact final tree | `C:\tmp\camera-adapter-linux-no-default-final.lcov`: 20,552 / 22,760 = **90.30%**; `C:\tmp\camera-adapter-linux-default-final.lcov`: 27,662 / 30,668 = **90.20%**. Core sources excluded. |
| Simulator suite | `./simulators/verify.ps1 -LinuxL2 -AravisInterface eth0` passed: ONVIF, TLS, H.264/H.265 MediaMTX, Toxiproxy, and Aravis L2 fake-camera acquisition. |
| Adapter RTSP decode | Pinned GStreamer validation image decoded both `rtsp://mediamtx:8554/camera` (H.264) and `camera-h265` (H.265). |
| Linux GenICam compile | `camera-adapter-aravis-validation` compiled `camera-adapter-genicam-discover` with `--features genicam`. |

Important Windows decision remains implemented: capture output uses the portable standard-library
no-overwrite persistence profile. There is no Windows-specific image-output DACL/helper code; only
the allowed state-directory protection remains.

## Core P1 work completed in this session

These changes are in the shared, dirty `core/` worktree and must remain together:

1. **Python shadow config ordering:**
   `libs/python/edgecommons/config/manager/shadow_config_manager.py` now reports only an accepted
   effective configuration. A rejected initial candidate neither reports nor subscribes; a rejected
   delta does not report as current. `libs/python/tests/test_lifecycle_contract.py` has the
   committed-generation regression. Focused P1 suite: 32 passed.
2. **TypeScript CommandInbox startup:**
   `libs/ts/src/commands.ts` adds bounded single-flight startup, queues deliveries until the
   subscription acknowledgement is active, removes a late subscription after timeout, and retains
   STOPPED when close wins the race. `libs/ts/test/commands.test.ts` adds the corresponding four
   race tests. Full TS evidence: 887 passed, 3 skipped; coverage statements 91.70%, branches
   84.79%, functions 91.86%, lines 93.03%; `npm run build` passed.
3. **Rust Core docs:**
   `libs/rust/src/lib.rs` accurately documents concurrent side-effect-free candidate validation
   with registration-ordered diagnostics. Rust P1 targeted test groups passed.
4. **Python native streaming close deadlock:**
   `libs/rust-streamlog/bindings/python/src/lib.rs` now takes the Python-visible service then drops
   it under `py.detach`, so its native worker can reacquire the GIL while close waits for join.
   `libs/python/tests/test_cloudwatch_durable.py` adds a subprocess-bounded disconnected-callback
   regression: close returns, sends no disconnected records, then reopening drains five recovered
   records. Validation passed:
   - `cargo check` in `core/libs/rust-streamlog/bindings/python`
   - rebuilt `target/wheels/edgestreamlog_native-0.1.0-cp39-abi3-win_amd64.whl` with Maturin and
     force-installed it into Python 3.14
   - focused close regression plus the existing durable-close test: 2 passed in 1.29s.

`core/libs/rust-streamlog/bindings/python/Cargo.lock` was regenerated by the binding build and is
intentionally present. The binding crate's formatter check still reports pre-existing formatting
outside the GIL-close patch; do not bulk-format that file without a separate review.

## Remaining work, in order

1. Re-run the Python full local coverage gate after the fixed native wheel is installed. The former
   run stalled in `TestCloudWatchTargetDurablePath::test_close_persists_backlog_without_draining`;
   the focused regression proves that defect fixed.

   ```powershell
   cd C:\Users\breis\source\edgecommons\core\libs\python
   py -3.14 -m pytest -m "not slow and not integration and not aws" --cov=edgecommons --cov-fail-under=90
   ```

   If Python sandbox process creation says access denied, rerun with the approved elevated `py -3.14`
   prefix. If the suite hangs again, use `-vv -o faulthandler_timeout=60` and inspect
   `C:\tmp\core-python-p1-full.log`; do not leave an orphaned `py.exe` process running.

2. Run the complete Rust Core 90% `cargo llvm-cov` gate and the relevant WSL Greengrass-feature
   build/tests. The P1 targeted Rust tests are green but this full gate is not yet recorded.
3. Run the P1 4-by-4 local MQTT requester/responder interop, including confirmed-PUBACK behavior,
   after bringing up EMQX from `core/test-infra/compose.yaml`.
4. Run the four-language deployed Greengrass IPC P1 interop on `lab-5950x`; then do the applicable
   deployed Greengrass regression, Kubernetes regression, physical ONVIF/PTZ and GenICam camera
   evidence, and the required platform/scale/soak checks. These are **unrun release gates**, not
   currently known implementation failures.
5. Before any completion claim, perform a final senior review of all dirty `core/` and
   `camera-adapter/` source/docs against `camera-adapter/IMPLEMENTATION_SPEC.md`, rerun affected
   quality gates on the exact final tree, and decide whether/when to commit. No commit has been
   created in this session.

## Reconstituting test infrastructure after reboot (no rebuild required)

Docker images and named Cargo caches were verified present. The simulator containers were recreated
successfully without `--build` immediately before this stop. After Docker Desktop is running, use:

```powershell
cd C:\Users\breis\source\edgecommons\camera-adapter
docker compose -f .\simulators\compose.yaml up -d --wait onvif-sim onvif-sim-tls mediamtx toxiproxy
docker compose -f .\simulators\compose.yaml --profile linux-l2 up -d aravis-fake
```

Cached images include `camera-adapter-simulators-onvif-sim:latest`,
`camera-adapter-simulators-aravis-fake:latest`, `camera-adapter-rtsp-validation`, and
`camera-adapter-aravis-validation`. Keep the named volumes
`camera-adapter-cargo-registry`, `camera-adapter-cargo-git`,
`camera-adapter-linux-llvm-cov-bin`, and `camera-adapter-integration-cov-target`; they make Linux
coverage/native validations reuse their Rust artifacts. Do **not** use `docker compose down -v`.

For simulator validation after reboot, run `./simulators/verify.ps1 -LinuxL2 -AravisInterface eth0`.
It may recreate containers but will use the cached images unless `--build` is explicitly requested.
